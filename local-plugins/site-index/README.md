# site-index

Not a fork of any upstream plugin — a small local component-only plugin providing the homepage's
alphabetical "phone book" index of every published note (German-aware sort, server-rendered from
`allFiles`, no client-side JS). Private, homepage-only; not published anywhere.
